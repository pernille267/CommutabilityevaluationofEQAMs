% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/number_formatting.R
\name{format_point_plus_interval}
\alias{format_point_plus_interval}
\title{Format Point Estimate and Confidence Interval}
\usage{
format_point_plus_interval(point, lower, upper, decimals = 2L)
}
\arguments{
\item{point}{Numeric vector. The point estimate value.}

\item{lower}{Numeric vector. The lower bound of the confidence interval.}

\item{upper}{Numeric vector. The upper bound of the confidence interval.}

\item{decimals}{Integer. The number of decimal places to round to. Defaults to 2.}
}
\value{
A character vector of formatted strings.
}
\description{
Formats a point estimate along with its lower and upper bounds into a single
string representation.
}
\details{
This function ensures rigid alignment for monospaced outputs (like console
logs or text tables). It processes the Point, Lower, and Upper columns
independently. If a value is missing (NA), it is replaced by whitespace
of the same width as the widest number in that column.
}
\examples{
# Example with partial NA values
pt <- c(1.23, 8, 1.341)
lo <- c(1.0191, NA, 1.29121)
up <- c(1.9, 131.22, NA)

# Output aligns columns, replacing NAs with spaces
format_point_plus_interval(pt, lo, up)
# Returns:
# "1.23 (1.02-  1.90)"
# "8.00 (    -131.22)"
# "1.34 (1.29-      )"
}
